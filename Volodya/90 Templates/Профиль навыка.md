---
type: skill
name: <% tp.file.title %>
level: 1
xp: 0
rank: Новичок
area: 
---
# <% tp.file.title %>

```dataviewjs
const p = dv.current();
const xp = p.xp || 0;
const maxXp = 1000;
const pct = Math.min(Math.round((xp / maxXp) * 100), 100);
const lvl = p.level || 1;
const colors = {1:'#888888',2:'#4caf50',3:'#2196f3',4:'#9c27b0',5:'#ff9800'};
const color = colors[Math.min(lvl, 5)];

const html = '<div style="padding:10px;background:var(--background-secondary);border-radius:8px;">'
    + '<b>Уровень ' + lvl + '</b> · ' + xp + ' XP'
    + '<div style="background:var(--background-modifier-border);border-radius:4px;height:10px;margin-top:6px;">'
    + '<div style="background:' + color + ';width:' + pct + '%;height:10px;border-radius:4px;"></div></div>'
    + '<div style="color:var(--text-muted);font-size:0.85em;margin-top:4px;">' + xp + ' / ' + maxXp + ' XP</div>'
    + '</div>';

dv.el('div', html, {cls: 'skill-bar'});
```

## Описание

## Как качается навык
- Задачи с тегом `#skill/<% tp.file.title.toLowerCase() %>`
- Сниппеты и решения

## Следующая цель

## Связанные проекты

```dataview
LIST
FROM "10 Projects"
WHERE contains(file.outlinks, this.file.link)
```

## Связанные ресурсы

```dataview
LIST
FROM "30 Resources"
WHERE contains(file.outlinks, this.file.link)
```