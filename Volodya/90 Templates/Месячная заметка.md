---
type: monthly
month: <% tp.date.now("YYYY-MM") %>
tags: [monthly]
---
# Месяц <% tp.date.now("MMMM YYYY") %>

## Главные результаты

## Закрытые квесты

```dataview
TABLE deadline as "Закрыт", reward_xp as "XP"
FROM ""
WHERE type = "project" AND status = "done"
  AND contains(string(deadline), "<% tp.date.now("YYYY-MM") %>")
  AND !contains(file.path, "90 Templates")
  AND !contains(file.path, "40 Archives")
SORT deadline DESC
```

## XP за месяц по навыкам

```dataviewjs
const month = "<% tp.date.now("YYYY-MM") %>";
const rewards = { simple: 10, normal: 20, major: 40, boss: 100 };

const tasks = dv.pages()
  .where(x =>
    x.type === "task" &&
    x.status === "done" &&
    x.deadline &&
    dv.date(x.deadline).toFormat("yyyy-MM") === month &&
    !x.file.path.includes("90 Templates") &&
    !x.file.path.includes("40 Archives")
  );

const bySkill = {};
for (const t of tasks) {
  const skill = t.skill || "без навыка";
  const xp = t.xp || rewards[t.task_type || "simple"] || 10;
  bySkill[skill] = (bySkill[skill] || 0) + xp;
}

const rows = Object.entries(bySkill).sort((a, b) => b - a);[1]
const total = rows.reduce((s, [, x]) => s + x, 0);

if (rows.length === 0) {
  dv.el("div", "Нет завершённых задач за этот месяц", {});
} else {
  dv.table(["Навык", "XP за месяц"], rows);
  dv.el("b", "Итого за месяц: " + total + " XP", {});
}
```

## Достижения месяца

```dataview
TABLE date as "Дата", achievement_xp as "XP"
FROM ""
WHERE type = "achievement"
  AND contains(string(date), "<% tp.date.now("YYYY-MM") %>")
  AND !contains(file.path, "90 Templates")
SORT date DESC
```

## Что улучшить в следующем месяце

## Цели на следующий месяц