---
type: monthly
tags: [monthly]
---
# Главные результаты

## Закрытые квесты

```dataviewjs
const monthKey = dv.current().file.name;
const rewards = { simple: 10, normal: 20, major: 40, boss: 100 };

const tasks = dv.pages()
  .where(x =>
    x.type === "task" &&
    x.status === "done" &&
    x.done_date &&
    dv.date(x.done_date).toFormat("yyyy-MM") === monthKey &&
    !x.file.path.includes("90 Templates") &&
    !x.file.path.includes("40 Archives")
  );

const modules = dv.pages()
  .where(x =>
    (x.type === "module" || x.type === "class" || x.type === "form") &&
    x.status === "done" &&
    x.done_date &&
    dv.date(x.done_date).toFormat("yyyy-MM") === monthKey &&
    !x.file.path.includes("90 Templates") &&
    !x.file.path.includes("40 Archives")
  );

const bySkill = {};

for (const t of tasks) {
  const skill = t.skill || "без навыка";
  const xp = t.xp || rewards[t.task_type || "simple"] || 10;
  bySkill[skill] = (bySkill[skill] || 0) + xp;
}

for (const m of modules) {
  const skill = m.skill || "без навыка";
  const xp = m.reward_xp || 0;
  bySkill[skill] = (bySkill[skill] || 0) + xp;
}

const rows = Object.entries(bySkill).sort((a, b) => b[1] - a[1]);
const total = rows.reduce((s, [, x]) => s + x, 0);

if (rows.length === 0) {
  dv.paragraph("Нет завершённых задач/модулей за этот месяц.");
} else {
  dv.table(["Навык", "XP за месяц"], rows);
  dv.paragraph("**Итого за месяц:** " + total + " XP");
}
```

## XP за месяц по навыкам

```dataviewjs
const monthKey = dv.current().file.name;
const rewards = { simple: 10, normal: 20, major: 40, boss: 100 };

const bySkill = {};

for (const t of dv.pages().where(x =>
  x.type === "task" &&
  x.status === "done" &&
  x.done_date &&
  dv.date(x.done_date).toFormat("yyyy-MM") === monthKey &&
  !x.file.path.includes("90 Templates") &&
  !x.file.path.includes("40 Archives")
)) {
  const skill = t.skill || "без навыка";
  const xp = t.xp || rewards[t.task_type || "simple"] || 10;
  bySkill[skill] = (bySkill[skill] || 0) + xp;
}

for (const m of dv.pages().where(x =>
  (x.type === "module" || x.type === "class" || x.type === "form") &&
  x.status === "done" &&
  x.done_date &&
  dv.date(x.done_date).toFormat("yyyy-MM") === monthKey &&
  !x.file.path.includes("90 Templates") &&
  !x.file.path.includes("40 Archives")
)) {
  const skill = m.skill || "без навыка";
  const xp = m.reward_xp || 0;
  bySkill[skill] = (bySkill[skill] || 0) + xp;
}

const rows = Object.entries(bySkill).sort((a, b) => b[1] - a[1]);
const total = rows.reduce((s, [, x]) => s + x, 0);

if (rows.length === 0) {
  dv.paragraph("Нет завершённых задач за этот месяц.");
} else {
  dv.table(["Навык", "XP за месяц"], rows);
  dv.paragraph("**Итого за месяц:** " + total + " XP");
}
```

## Достижения месяца

```dataviewjs
const monthKey = dv.current().file.name;

const rows = dv.pages()
  .where(x =>
    x.type === "achievement" &&
    x.date &&
    dv.date(x.date).toFormat("yyyy-MM") === monthKey &&
    !x.file.path.includes("90 Templates")
  )
  .sort(x => x.date, "desc")
  .map(x => [x.file.link, dv.date(x.date).toFormat("yyyy-MM-dd"), x.achievement_xp || 0]);

if (rows.length === 0) {
  dv.paragraph("Нет достижений за этот месяц.");
} else {
  dv.table(["Достижение", "Дата", "XP"], rows);
}
```

## Что улучшить в следующем месяце

## Цели на следующий месяц