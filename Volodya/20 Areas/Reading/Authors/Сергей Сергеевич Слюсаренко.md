---
type: author
country: BY (Беларусь)
born: 1955-05-16
died:
genres:
  - научная фантастика
  - постапокалипсис
  - боевая фантастика
tags:
  - author
aliases:
  - Сергей Сергеевич Слюсаренко
  - Sergey Slyusarenko
---

# Сергей Сергеевич Слюсаренко

## Кратко
<p align="justify">Сергей Слюсаренко — белорусский писатель-фантаст, поэт и физик, кандидат физико-математических наук. В 1972 году окончил школу с золотой медалью, учился на физических факультетах БГУ и ХГУ. С 1981 по 2015 год работал в Институте физики Академии наук Украины в Киеве, где занимался голографией, лазерной физикой и жидкими кристаллами. Дебютировал как писатель в 2003 году с рассказом "Воля к полёту", участвовал в межавторских проектах "S.T.A.L.K.E.R.", "Кремль 2222", "Дорога к Марсу".</p> 
### Цикл "Вадим Малахов"(КС)
<p align="justify">Слюсаренко создал серию из шести романов о сталкере Вадиме Малахове, которые публиковались с 2010 по 2019 год в рамках книжной серии S.T.A.L.K.E.R. и подсерии "Новая Зона". Главный герой цикла — руководитель группы "Табигон", который сталкивается с опасными приключениями в Зоне отчуждения. После катастрофического провала операции "Золотая сфера" группа расформировывается, и Малахову приходится скрываться под чужим именем, работая частным детективом, но он не оставляет попыток разобраться в причинах провала.</p>
## Сводка по автору

```dataviewjs
const a = dv.current();

function fmt(v, empty = "—") {
  if (v == null || v === "") return empty;
  if (Array.isArray(v)) return v.length ? v.join(", ") : empty;
  return String(v);
}

function fmtDate(v, empty = "—") {
  if (!v) return empty;
  try {
    return dv.func.dateformat(dv.date(v), "yyyy-MM-dd");
  } catch (e) {
    return empty;
  }
}

const books = dv.pages('"Volodya/20 Area/Reading/Books"')
  .where(b => b.type === "book" && b.author && String(b.author.path || b.author) === a.file.path)
  .array();

function num(v, def = 0) {
  const n = Number(v);
  return Number.isFinite(n) ? n : def;
}

function calcBookXP(book) {
  const pages = num(book.pages);
  if (pages <= 0) return 0;

  const current = Math.min(num(book.current_page), pages);
  const progress = current / pages;
  const baseXP = 100;
  const sizeCoef = num(book.size_coef, 1);
  const difficultyCoef = num(book.difficulty_coef, 1);
  const finishBonus = String(book.status || "") === "done" ? num(book.finish_bonus, 0) : 0;

  return Math.round(progress * baseXP * sizeCoef * difficultyCoef + finishBonus);
}

const doneBooks = books.filter(b => String(b.status || "") === "done");
const ratedBooks = books.filter(b => num(b.rating) > 0);
const avgRating = ratedBooks.length
  ? (ratedBooks.reduce((s, b) => s + num(b.rating), 0) / ratedBooks.length).toFixed(2)
  : "—";
const totalXP = doneBooks.reduce((s, b) => s + calcBookXP(b), 0);

dv.table(
  ["Параметр", "Значение"],
  [
    ["Страна", fmt(a.country)],
    ["Дата рождения", fmtDate(a.born)],
    ["Дата смерти", fmtDate(a.died)],
    ["Жанры", fmt(a.genres)],
    ["Книг в vault", books.length],
    ["Прочитано", doneBooks.length],
    ["Средний рейтинг", avgRating],
    ["Суммарный XP", totalXP]
  ]
);
```
## Что у меня прочитано
```dataview
TABLE status AS "Статус", rating AS "Рейтинг", done_date AS "Дата", pages AS "Стр."
WHERE type = "book"
AND contains(author, this.file.link)
AND !contains(file.path, "90 Templates")
AND !contains(file.path, "40 Archives")
SORT done_date DESC
```

## Заметки
- 

## Похожие авторы

## Связь
- Навык: [[Reading]]