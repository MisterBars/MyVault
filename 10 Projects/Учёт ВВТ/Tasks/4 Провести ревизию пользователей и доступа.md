---
type: task
status: active
done_date:
skill:
  - vba
task_type: normal
project: "[[Учёт ВВТ]]"
deadline:
tags:
  - task
---
# ## Провести ревизию пользователей и доступа

## Что нужно сделать
- [ ] Проверить и заполнить карточки для:
    - [ ] `Users`
    - [ ] `UserServices`
    - [ ] `UserSessions`
- [ ] Уточнить бизнес-логику ролей, ограничений и bootstrap admin.
- [ ] В карточке `UserSessions` явно описать `SessionToken`, `LastPing`, `SessionStatus`, `WorkbookHost`.
- [ ] В карточке `ChangeRequests` позже связать `LockToken` с `UserSessions.SessionToken`, но тут уже зафиксировать это на уровне общей схемы.
## Критерии готовности
- Карточки `Users`, `UserServices`, `UserSessions` совпадают со структурой v7.
- В карточках отражены реальные поля безопасности и сессий из progress/v7.
- В `Users` отражён self-FK `CreatedByUserID -> Users.UserID`.
- В `UserSessions` отражены статусы `Active / Closed / Expired / Crashed`.
## Заметки по ходу

## Итог